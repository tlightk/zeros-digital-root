{
  "name": "zeros_digital_root",
  "version": "1.0.2",
  "description": "A function for calculating the digital root of any positive integer",
  "main": "digital-root.js",
  "scripts": {
    "pre-commit": "pretty-quick --staged --no-restage",
    "pre-push": "npm run test",
    "postinstall": "husky install",
    "prepublishOnly": "pinst --disable",
    "postpublish": "pinst --enable",
    "preversion": "npm test",
    "postversion": "git push origin --tags --no-verify",
    "test": "mocha"
  },
  "author": "klayxi",
  "license": "ISC",
  "keywords": [
    "digital",
    "root"
  ],
  "devDependencies": {
    "chai": "^4.2.0",
    "husky": "^4.3.5",
    "mocha": "^8.2.1",
    "pinst": "^2.1.1",
    "prettier": "^2.2.1"
  }
}
